 PRD EVOLUCIONADO: Decoupled Digit & Vector Field AnimationDirigido a: Agente de Desarrollo (Google AI Studio)Contexto: Sección de estadísticas de alta conversión para "Repliyo" (Inbox IA).Referencia visual: Movimiento tipo "asynchronous digit morphing" y fondo de campo vectorial reactivo.1. <analisis_de_dominio>: Narrativa de DatosImplementar un ciclo de 4 estadísticas clave que rotan cada 5 segundos:"80%" -> Menos tiempo respondiendo."2 min" -> Tiempo de respuesta promedio."5x" -> Más leads gestionados."24/7" -> Cobertura total IA.2. <especificaciones_tecnicas>: El Core de la Animación"Desarrolla la sección siguiendo estas reglas de animación avanzada para los números":A. Desacoplamiento de Dígitos (Granularidad)Independencia Total: No animes el número como un "string". Cada carácter (el '8', el '0', el '%') debe ser un componente independiente con su propio ciclo de vida.Entrada Asíncrona (Staggered): * El primer dígito debe entrar desde la izquierda.El segundo dígito debe entrar desde abajo.El símbolo (%) debe entrar desde la derecha.Aplica un delay (retraso) de 0.1s entre cada uno para crear el efecto de "ensamblaje".B. Física de Movimiento (Directional Flow)Efecto de Salida: Cuando el número cambie (ej. de 80 a 2), los dígitos viejos no deben simplemente desaparecer; deben "salir" disparados en la dirección opuesta a la que entraron (ej. el '8' sale hacia la derecha, el '0' hacia arriba).Spring Dynamics: Utiliza stiffness: 100 y damping: 15. Esto hará que cuando el número entre, tenga un pequeño rebote elástico (overshoot) antes de fijarse.C. Campo Vectorial Reactivo (Background)Lógica Matemática: Crea una malla de líneas donde cada línea calcule su rotación $\theta$ basada en la posición del mouse $(m_x, m_y)$ y la posición de la línea $(l_x, l_y)$ mediante la función:$$\theta = \operatorname{atan2}(m_y - l_y, m_x - l_x)$$Impacto del Número: Cuando un dígito entra o cambia, debe generar una "onda de choque" que afecte temporalmente a las líneas cercanas en un radio de 200px.3. <fase_tdd_red>: Criterios de Calidad SeniorPrueba de Sincronía: El texto descriptivo ("Menos tiempo respondiendo") debe aparecer solo después de que el último dígito del número se haya asentado.Prueba de Rendimiento: La animación debe ejecutarse en el main thread sin bloqueos. Usa layout transitions de Framer Motion para asegurar fluidez.Visual Polish: Aplica un ligero desenfoque de movimiento (blur) muy sutil (2px) a los números mientras están en tránsito para simular velocidad real.4. <instrucciones_implementacion> (Prompt Directo para la IA)"Actúa como Senior Frontend Engineer. Implementa un componente de React que use framer-motion. Quiero una sección de estadísticas donde los números se 'ensamblen' asíncronamente: el primer dígito entra desde la izquierda, el segundo desde abajo y el tercero desde la derecha. Usa física de resortes para el rebote. El fondo debe ser una malla de líneas que sigan al puntero usando la lógica de campo vectorial del video de referencia. Los datos a ciclar son: [80%, 2 min, 5x, 24/7]. Asegúrate de que cada dígito sea un elemento motion.span independiente."¿Qué corregimos ahora?He añadido el detalle de que los números se rompan por cifras y entren por distintas direcciones. Esto es lo que le da el toque "Apple-like" o de "High-end landing".¿Te gustaría que te genere el código TypeScript específico para el componente DigitAssembler que maneja precisamente esa entrada asíncrona por direcciones?